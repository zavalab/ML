_, _, _, model = main(k=1,
                      image_size=100,
                      learning_rate=0.0005,
                      batch_size=256,
                      model_checkpoint='loss',
                      num_filters=32,
                      train=False)
