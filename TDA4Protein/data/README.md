# Data Folder

This folder contains the canonical train/test split files and the protein surface CSV files used by the pipeline.

Provided layout:

```text
data/protein_csvs/<protein_id>.csv
data/splits/training.txt
data/splits/testing.txt
```

The feature, descriptor, and results folders are generated by the scripts when the pipeline is run.
